// @desc: WorkInProgress
// @lastWritten: 2025-07-03
// @upToDate: true
using CastL.Gameplay;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CastL.Managers
{
    public class TowerManager : MonoBehaviour
    {/*
        public static TowerManager Instance;

        private void Awake()
        {
            Instance = this;
        }
        

        public void StartMines()
        {
            StoneMine[] mines = FindObjectsOfType<StoneMine>();
            foreach (StoneMine mine in mines)
            {
                mine.StartMining();
            }
        }
        public void StopMines()
        {
            StoneMine[] mines = FindObjectsOfType<StoneMine>();
            foreach (StoneMine mine in mines)
            {
                mine.StartMining();
            }
        }
        */
    }
}
